Graduate feature: $ARGUMENTS

1. Check /sandbox/$ARGUMENTS exists
2. Verify:
   - README.md present
   - All tests passing (if any)
   - No external dependencies outside folder
3. List all files to be moved
4. Show new paths in /skills/$ARGUMENTS/
5. Wait for approval before moving